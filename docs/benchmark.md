# Benchmark

The benchmark script measures candidates processed per second while retaining the
same ranking path used for final submission generation.

```bash
python benchmark.py --candidates data/raw/candidates/candidates.jsonl --limit 1000
```
