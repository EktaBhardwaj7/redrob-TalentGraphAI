# Diagnostics

Diagnostics modules summarize score distribution, feature statistics, runtime,
candidate-level evidence, ontology coverage, and validation warnings.

Run:

```bash
python run_diagnostics.py --candidates data/raw/candidates/candidates.jsonl --out data/output/diagnostics/report.json
```
