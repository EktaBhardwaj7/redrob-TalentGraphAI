# Methodology

The ranker favors explicit, reviewable evidence over opaque embeddings. Evidence
is extracted through a capability ontology, converted into independent feature
scores, then combined with deterministic weights. Reasoning is generated only
from observed evidence, keeping explanations grounded in candidate data.
